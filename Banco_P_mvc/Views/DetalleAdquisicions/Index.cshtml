@model IEnumerable<Banco_P_mvc.Models.DetalleAdquisicion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Nom_Cli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Seguro.Num_Tar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fec_adq)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Nom_Cli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seguro.Num_Tar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fec_adq)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Det_Adq }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Det_Adq }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Det_Adq })
        </td>
    </tr>
}

</table>
