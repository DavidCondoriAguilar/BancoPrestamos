@model IEnumerable<Banco_P_mvc.Models.DetalleSolicitud>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Nom_Cli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prestamo.Cod_Suc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fec_Sol)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Nom_Cli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prestamo.Cod_Suc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fec_Sol)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Solicitud }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Solicitud }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Solicitud })
        </td>
    </tr>
}

</table>
