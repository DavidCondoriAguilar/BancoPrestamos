@model IEnumerable<Banco_P_mvc.Models.DetalleApertura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Nom_Cli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cuenta.Ruc_Cli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fec_Ape)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Nom_Cli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cuenta.Ruc_Cli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fec_Ape)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Ape }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Ape }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Ape })
        </td>
    </tr>
}

</table>
